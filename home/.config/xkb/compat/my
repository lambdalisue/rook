default partial xkb_compatibility "latch_r" {
    virtual_modifiers  Shift, Control;

    interpret.repeat = False;

    interpret Shift_R {
      action= LatchMods(modifiers=Shift);
    };

    interpret Control_R {
      action= LatchMods(modifiers=Control);
    };

};
